if (isMoving) {
    if (path_index == -1) {
        isMoving = false;
        scrHeroMoveFinish(id);
    }
    exit;
}

switch(state){
    case "Start turn":
        if(army == BLUEARMY){
            // show movement path options
        }
        
        break;
}